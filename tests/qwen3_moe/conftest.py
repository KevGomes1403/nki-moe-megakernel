"""Pytest fixtures for qwen3_moe tests.

Currently empty: the strict-tolerance attention test runs via ``nki.simulate``
(CPU NumPy simulation), so no distributed/XLA setup is required.

This file is kept in case future tests need shared fixtures.
"""
